<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.makesystem.agenda2">

    <application>
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/view_new_people"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agenda2">
        <activity
            android:name=".ui.activity.FormularioAluno"
            android:label="@string/title_activity_formulario_aluno"
            android:theme="@style/Theme.Agenda2.NoActionBar"/>


        <activity android:name=".ui.activity.ListaAlunosActivityMain"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Agenda2.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>